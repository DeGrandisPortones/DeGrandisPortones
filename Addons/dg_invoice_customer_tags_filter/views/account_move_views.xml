<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_invoice_filter_customer_tags" model="ir.ui.view">
        <field name="name">account.move.select.customer.tags</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_invoice_filter"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="partner_category_id" string="Etiquetas de Cliente"/>
            </field>
            <xpath expr="//group/filter[@name='salesperson']" position="before">
                <filter name="group_by_partner_category_id"
                        string="Etiqueta de Cliente"
                        context="{'group_by': 'partner_category_id'}"/>
            </xpath>
        </field>
    </record>

    <record id="view_invoice_tree_customer_tags" model="ir.ui.view">
        <field name="name">account.move.tree.customer.tags</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <field name="move_type" position="after">
                <field name="partner_category_id"
                       string="Etiquetas de Cliente"
                       widget="many2many_tags"
                       optional="hide"/>
            </field>
        </field>
    </record>

</odoo>
