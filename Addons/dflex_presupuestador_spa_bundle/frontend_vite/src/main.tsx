import React from "react";
import ReactDOM from "react-dom/client";
import App from "./ui/App";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("dflex-presupuestador-root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
